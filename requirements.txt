# CLI & Interface
typer>=0.9.0
colorama>=0.4.6
rich>=13.0.0

# AI Engine (Heavy stuff)
# Nota: Si el usuario solo quiere el modo básico, podría omitir esto,
# pero para facilitar la instalación, lo ponemos aquí.
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
